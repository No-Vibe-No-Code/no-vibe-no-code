{
  "name": "no-vibe-no-code",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test:workspace": "npm run build:assets && node tests/workspace.mjs",
    "test:admin": "npm run build:assets && node tests/admin.mjs",
    "build:assets": "esbuild public/app.js public/admin.js public/shared.js public/product-motion.js public/home.js public/workspace-shell.js public/workspace.js public/profile.js public/gallery.js public/form.js public/project.js public/members.js public/nfc.js public/vote.js public/mobile-shell.js --bundle --minify --format=iife --outdir=public/min --entry-names=[name]",
    "deploy": "npm run build:assets && wrangler deploy",
    "check": "npm run build:assets && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "wrangler": "^4.127.1"
  },
  "dependencies": {
    "animejs": "^4.5.0",
    "marked": "^18.0.13"
  }
}
