{
  "name": "no-vibe-no-code",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build:assets": "esbuild public/app.js public/admin.js public/shared.js public/product-motion.js public/home.js public/profile.js public/gallery.js public/form.js public/project.js public/members.js public/nfc.js --minify --format=iife --outdir=public/min --entry-names=[name]",
    "deploy": "npm run build:assets && wrangler deploy",
    "check": "npm run build:assets && wrangler deploy --dry-run"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "wrangler": "^4.127.1"
  },
  "dependencies": {
    "marked": "^18.0.13"
  }
}
